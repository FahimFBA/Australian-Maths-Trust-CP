#include<bits/stdc++.h> // include a lot of functionality of C++. This is a combination of many libraries
using namespace std;

int main()
{
    freopen("addin.txt" , "r" , stdin); // r = read format
    freopen("addout.txt" , "w" , stdout); // w = write format

    int num1, num2;
    cin >> num1 >> num2;
    int sum = num1 + num2;
    cout << sum << endl;
    return 0;
}
